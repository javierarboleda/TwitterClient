<resources>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
        <item name="colorPrimary">#0099d4</item>
        <item name="colorPrimaryDark">#0087ba</item>
        <item name="colorAccent">#00aced</item>
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="TimelineTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorColor">@color/white</item>
        <item name="tabSelectedTextColor">@color/white</item>
    </style>


    <style name="userProfileTextViews">
        <item name="android:layout_below">@id/tvDescription</item>
        <item name="android:layout_marginTop">6dp</item>
        <item name="android:layout_marginBottom">12dp</item>
        <item name="android:fontFamily">sans-serif-condensed</item>
    </style>

    <style name="userProfileCountTextViews" parent="userProfileTextViews">
        <item name="android:layout_marginLeft">6dp</item>
        <item name="android:textColor">@color/whiteFirstShade</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="userProfileFollowLabelTextViews" parent="userProfileTextViews">
        <item name="android:textColor">@color/WhiteSecondShade</item>
        <item name="android:layout_marginLeft">4dp</item>
    </style>

    <style name="CustomActionBar" parent="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <!-- Support library compatibility -->
        <item name="windowActionBarOverlay">true</item>
    </style>

</resources>
